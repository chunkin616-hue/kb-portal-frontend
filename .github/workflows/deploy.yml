name: Deploy Frontend

on:
  push:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - uat
          - production

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: ${{ github.event.inputs.environment || 'dev' }}
    steps:
      - uses: actions/checkout@v4
      
      - name: Set deployment target
        run: |
          case ${{ env.ENVIRONMENT }} in
            dev)
              echo "DEPLOY_PORT=3003" >> $GITHUB_ENV
              echo "Deploying to DEV environment (port 3003)"
              ;;
            uat)
              echo "DEPLOY_PORT=3004" >> $GITHUB_ENV
              echo "Deploying to UAT environment (port 3004)"
              ;;
            production)
              echo "DEPLOY_PORT=3005" >> $GITHUB_ENV
              echo "Deploying to Production environment (port 3005)"
              ;;
          esac
      
      - name: Deploy to server
        run: echo "Deploying to 192.168.140.149:${{ env.DEPLOY_PORT }}"
